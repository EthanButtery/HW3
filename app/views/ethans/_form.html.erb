<%= form_with(model: ethan, local: true) do |form| %>
  <% if ethan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ethan.errors.count, "error") %> prohibited this ethan from being saved:</h2>

      <ul>
      <% ethan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :login %>
    <%= form.text_field :login %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
